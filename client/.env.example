# Copy to .env (local) or set in Vercel project env vars.
# URL of the deployed game server (Render/Fly.io). No trailing slash.
# Leave unset locally to default to http://<host>:3001
VITE_SERVER_URL=
